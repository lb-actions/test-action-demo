name: test
on:
  pull_request:
    branches: [ main ]
    types: [closed]
  issue_comment:
    types: [created]
  workflow_dispatch:
jobs:
  build:
    runs-on: [self-hosted, Linux, insight, X64]
    steps:
    - name: Just test
      run: |
        echo "test"
